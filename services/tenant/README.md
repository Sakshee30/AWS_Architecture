# Tenant service

Tenant/workspace lifecycle application service.

It enforces permission checks, active-tenant state, normalized workspace slugs, uniqueness through a repository port, and server-side tenant/workspace isolation before returning resources.

import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
test('frontend defines required security headers and avoids provider leakage in dashboard',async()=>{const next=await readFile('apps/web/next.config.mjs','utf8');for(const h of ['Content-Security-Policy','Strict-Transport-Security','X-Content-Type-Options','Referrer-Policy','Permissions-Policy'])assert.ok(next.includes(h));const dash=await readFile('apps/web/src/app/dashboard/page.tsx','utf8');for(const provider of ['Redis','Kafka','SQS'])assert.equal(dash.includes(provider),false);});
